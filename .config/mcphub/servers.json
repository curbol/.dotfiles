{
  "mcpServers": {
    "shortcut": {
      "env": {
        "SHORTCUT_API_TOKEN": "$SHORTCUT_API_TOKEN"
      },
      "args": [
        "-y",
        "@shortcut/mcp@latest"
      ],
      "command": "npx"
    }
  },
  "nativeMCPServers": []
}
