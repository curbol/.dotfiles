-- Keep only personal input overrides here. Everything else this machine used to
-- set (repeat_rate, repeat_delay, numlock_by_default, touchpad clickfinger and
-- scroll_factor, per-terminal scroll_touchpad) now matches Omarchy's defaults.

hl.config({
  input = {
    accel_profile = "flat",
  },
})
